using UnityEngine;

public class ObjectControll : MonoBehaviour
{
 //   void OnBecameInvisible()
	//{
	//	// Если статус игры не "gameOver" и не "пауза", то устанавливаем новые позицию и размер
	//	if(!PlayerControll.instance.gameOver && !GameManager.instance.p_gui.isPause)
	//		ObjectsGenerator.instance.setObjectPositionAndScale(ObjectsGenerator.instance.objectPull);
	//}
}
